Query:
    type: object
    config:
        description: 'Just one query'
        fields:
            allPatients:
                type: '[Patient]'
                resolve: "@=resolver('all_patients', [args])"